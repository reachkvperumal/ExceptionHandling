openapi: 3.0.1
info:
  title: Demo Generation
  description: This is a sample API used to illustrate the tooling around an API-first approach.
  version: 1.0.0
  x-audience: moderate
  x-api-id: 58febb0f-49a1-48f1-a060-b3b1a615f465
  contact:
    name: KV Perumal

paths:
  /:
    put:
      responses:
        '200':
          description: OK

components:
  schemas:
    WriteDemoRes:
      type: object
      properties:
        firstName:
          type: string
        lastName:
          type: string

    ReadDemoRes:
      type: object
      properties:
        name:
          type: string
        accessKey:
          type: string
        eventName:
          type: string
        eventId:
          type: string

    DummyModel:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WriteDemoRes'